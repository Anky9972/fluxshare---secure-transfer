# Gemini API Key for AI features (optional)
GEMINI_API_KEY=your_api_key_here

# P2P Configuration
# Comma separated list of STUN/TURN servers for WebRTC
VITE_ICE_SERVERS=stun:stun.l.google.com:19302,stun:global.stun.twilio.com:3478

# P2P Share PeerJS Server Configuration (optional - leave empty for default cloud server)
# VITE_PEER_HOST=your-peerjs-server.com
# VITE_PEER_PORT=443
# VITE_PEER_PATH=/
# VITE_ENV=production

# Broadcast Hub PeerJS Server Configuration
# IMPORTANT: The public server 0.peerjs.com does NOT support peer listing
# You MUST deploy your own PeerJS server for the broadcast feature to work
# FluxShare includes a local server at server/peer-server.cjs with peer listing enabled
# For production, deploy this server to Railway, Render, or any Node.js hosting platform

# After deploying your server, update these values:
VITE_BROADCAST_PEER_HOST=your-app.railway.app
VITE_BROADCAST_PEER_PORT=443
VITE_BROADCAST_PEER_PATH=/

# Make sure to enable peer listing on your server for the broadcast feature to work
# The local server at server/peer-server.cjs has peer listing enabled by default

# Environment type (development or production)
# VITE_ENV=production
